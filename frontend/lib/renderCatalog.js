/**
 * renderCatalog.js — DEPRECATED
 *
 * All interactive components (Flashcards, MCQ, Quiz, Actions) are now
 * delivered via native AI SDK tool calling (showFlashcards, showMCQ,
 * showQuiz, showActions). This file is kept as a stub to avoid broken
 * imports during the transition and can be deleted once confirmed clean.
 */
